# انتقال الحسابات إلى مصادقة محلية مستقلة

## النموذج الجديد

تعتمد زُهيرة الآن على **حساب محلي بالبريد الإلكتروني وكلمة المرور**. تحفظ قاعدة البيانات كلمة مرور مشفرة ومملّحة فقط في `password_hash`، بينما تحفظ الجلسة الموقعة رقم المستخدم المحلي في ملف تعريف ارتباط آمن. لا تستدعي مسارات الإنتاج الفعالة بوابة Manus أو خادم OAuth الخاص بها.

## معالجة الحسابات القديمة

السجلات التي أنشئت سابقاً عبر Manus تبقى في جدول `users` وسجلات الدورة المرتبطة بها تبقى كما هي؛ لا تُحذف تلقائياً. لا يمكن تحويلها تلقائياً إلى كلمة مرور محلية، لأن التطبيق لم يكن يملك كلمة المرور الأصلية ولا ينبغي له إنشاء كلمة مرور أو ربط سجل بحساب يملك البريد نفسه دون تحقق مستقل.

في هذه المرحلة، لا تُقبل جلسات Manus القديمة. إذا وُجدت بيانات فعلية لحساب قديم، فالخيار الآمن هو تنفيذ عملية استعادة يدوية موثقة بعد إضافة تحقق ملكية بريد إلكتروني، ثم ربط الحساب المحلي المُتحقق منه بسجل المستخدم القديم. لا توجد بيانات مستخدمات إنتاجية مؤكدة لترحيلها حالياً، لذلك لا ينفذ هذا الإصدار أي ربط تلقائي أو نقل قد يعرّض الخصوصية للخطر.

## ما يلزم قبل أي استعادة مستقبلية

لاستعادة سجلات OAuth قديمة بأمان، يجب إضافة تحقق بريد إلكتروني مستقل، وطلب موافقة صاحبة الحساب، وتسجيل عملية الربط. لا يكفي تطابق البريد وحده لأنه قد يسمح بالاستيلاء على بيانات صحية خاصة.
