# دليل التشغيل الأمني لزُهيرة

## نطاق الرصد

يُراجع مالك التطبيق بانتظام مجموعات أخطاء وقت التشغيل في Vercel، مع التركيز على مسارات تسجيل الدخول، تغيير كلمة المرور، قفل الجهاز، وروابط مشاركة التقارير. يجب التعامل مع الرسائل وسياق الخطأ كبيانات حساسة؛ لا تُضاف نصوص SQL أو القيم أو البريد الإلكتروني أو رموز المشاركة إلى السجلات.

تُستخدم `/api/health` فقط لإشارة جاهزية عامة. النتيجة المتوقعة هي `{ "status": "ok" }`، ولا ينبغي أن تكشف المسارات العامة عن اسم موفر قاعدة البيانات أو تفاصيل الاتصال أو الإعدادات.

## الاستجابة للحوادث

عند ظهور مجموعة أخطاء صلاحيات قاعدة بيانات، يُراجع آخر ترحيل وامتيازات الدور `zuhiera_app` وسياسات RLS، ثم يُجرى تحقق قراءة فقط من الصلاحيات قبل منح أقل مستوى وصول مطلوب. عند ظهور أعطال تسجيل دخول أو زيادة في `LOGIN_THROTTLED`، يُراجع الحد الزمني للمحاولات دون جمع عناوين IP خام أو تخزينها.

## تدوير الأسرار والجلسات

عند الاشتباه في كشف مفتاح جلسة، يُنشأ مفتاح JWT عشوائي جديد بطول لا يقل عن 32 حرفاً داخل إعدادات Vercel فقط، ثم يُعاد النشر. يؤدي تغيير المفتاح إلى رفض الجلسات الموقعة بالمفتاح القديم. تغيير كلمة المرور يرفع إصدار جلسة الحساب ويُلغي الجلسات الأخرى تلقائياً مع إصدار جلسة جديدة للجهاز الحالي.

## التحقق قبل النشر

قبل كل نشر: شغّل `pnpm check` و`pnpm test` و`pnpm build` و`pnpm audit --prod`. لا يُقبل نشر بوجود تنبيه عالي أو حرج في اعتماديات الإنتاج دون قرار موثق وخطة معالجة.
